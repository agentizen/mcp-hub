# Go artifacts
dispatcher/dispatcher
dispatcher/coverage.out
dispatcher/coverage.html
*.test
*.out

# Editor / OS
.idea/
.vscode/
*.swp
.DS_Store

# Node / Python build artifacts (installed inside the Docker image;
# package.json + package-lock.json are the only source of truth).
node/node_modules/
python/**/.venv/
__pycache__/
*.pyc

# Build / dist
dist/
build/

# Operator-local runtime config. Copy config.example.yaml → config.yaml
# and customize; never commit your production config (it may contain
# sensitive subprocess env vars or proprietary handle names).
config.yaml

# Local env file (copy .env.example → .env and fill in secrets).
.env
